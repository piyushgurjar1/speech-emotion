tensorflow-cpu==2.18.0  # Use CPU-only version (saves ~200MB)
librosa==0.10.1         # Lighter version without full dependencies
numpy==1.26.4           # Compatible version (v2.x is larger)
scipy==1.13.0           # Minimal required version
h5py==3.11.0            # Reduced size version
Flask==3.0.2            # Current stable
soundfile==0.12.1       # Lighter audio handling
scikit-learn==1.4.2     # Minimal ML utilities
joblib==1.3.2           # Required by scikit-learn
numba==0.58.1           # Compatible with librosa 0.10.x
llvmlite==0.41.1        # Matches numba 0.58
serverless-wsgi==3.0.2  # Compatible middleware