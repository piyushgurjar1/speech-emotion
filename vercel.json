{
    "builds": [
      {
        "src": "api/index.py",
        "use": "@vercel/python",
        "config": {
          "maxLambdaSize": "300mb",
          "installCommand": "pip install --no-cache-dir --no-binary numba -r requirements.txt"
        }
      }
    ],
    "routes": [
      {"src": "/(.*)", "dest": "api/index.py"}
    ]
  }